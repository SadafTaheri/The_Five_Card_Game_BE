import { Request, Response, NextFunction } from 'express';
import { setAllAbilities } from '../Models/abilitiesModels';

export function getAllAbilities(req: Request, res: Response, next: NextFunction) {

    // setAllAbilities()
    // .then({rows})
}

